#include<bits/stdc++.h>
#include<graphics.h>
using namespace std;

int main()
{
    int gd = DETECT, gm;
    initgraph (&gd, &gm, "");
    putpixel(400, 300, WHITE);
    getch();
    line(300,300,456,234);
    getch();
    circle(200,150,100);
    getch();
}
